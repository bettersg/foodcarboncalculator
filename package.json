{
  "name": "foodcarboncalculator",
  "version": "1.0.0",
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "reset": "lerna clean -y && rm -rf node_modules"
  },
  "private": true
}
